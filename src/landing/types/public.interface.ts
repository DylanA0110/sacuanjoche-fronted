// Interfaces para los endpoints públicos de la landing page

export interface FlorPublic {
  idFlor: number;
  nombre: string;
  color: string;
}

export interface AccesorioPublic {
  idAccesorio: number;
  descripcion: string;
  categoria: string;
}

